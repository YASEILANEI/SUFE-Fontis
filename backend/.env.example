# 环境变量示例：复制为 .env 并填入真实值
# 推荐：DeepSeek 官方接口（海外服务器如 Render / HF Space 美区可访问，无地域限制）
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=sk-xxxx
OPENAI_MODEL=deepseek-chat

# 备选：opencode（OpenAI 兼容接口）
# 注意：opencode.ai 的 /zen/go/v1 存在地域限制（仅国内/特定区域 IP 可达），
# 部署到海外服务器会返回 404 导致"生成回答失败"，需改用官方或海外可达的接口。
# OPENAI_BASE_URL=https://opencode.ai/zen/go/v1
# OPENAI_MODEL=deepseek-v4-flash

# 向量检索（可选，装 requirements-vector.txt 后生效）
EMBED_MODEL=bge-m3
